/**
 * Logging-related utility classes for the application.
 *
 * Note: {@code simplelogger.properties} (SLF4J's config file) must stay
 * in the default package at the classpath root -- SLF4J only looks for
 * it there, so it cannot be moved into this package.
 */
package poscs.logs;
